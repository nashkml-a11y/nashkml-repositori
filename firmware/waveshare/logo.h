// ============================================================
// PLACEHOLDER — logo.h
// Reemplazar por el array RGB565 real del logo Dragon Ball.
// Formato: uint16_t logo_data[LOGO_W * LOGO_H]
// ============================================================
#pragma once
#include <stdint.h>

#define LOGO_W 120
#define LOGO_H 120

// Array vacío — sustituir por el logo real
static const uint16_t logo_data[LOGO_W * LOGO_H] = { 0 };
