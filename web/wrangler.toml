name = "buscador-web"
pages_build_output_dir = "dist"

# VITE_API_URL se define como variable de entorno del proyecto de Cloudflare
# Pages (Settings → Environment variables), no aquí: Vite la incrusta en el
# build en tiempo de compilación, así que tiene que existir ANTES de
# `npm run build` (Pages la inyecta automáticamente si la configuras allí).
